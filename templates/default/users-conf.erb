# THIS FILE IS MANAGED BY CHEF. MODIFIED CHANGES WILL BE OVERWRITTEN DURING THE NEXT CHEF-CLIENT RUN.
/**
 * The example user 'icingaadmin' and the example
 * group 'icingaadmins'.
 */

object UserGroup "icingaadmins" {
  display_name = "Icinga 2 Admin Group"
}

<% @users.each do |usr, email| %>
  object User "<%= usr %>" {
    import "generic-user"
    display_name = "<%= usr %>"
    groups = [ "icingaadmins" ]
    email = "<%= email %>"
  }
  
<% end %>